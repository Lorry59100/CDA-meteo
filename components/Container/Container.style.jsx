import { StyleSheet } from "react-native";

export const ContainerStyle = StyleSheet.create({
    background: {
        width: "100%",
        height: "100%" 
    },
    container: {
        flex: 1
    }
});
